# API Contracts

Endpoint/interface signatures with invariants that a type signature can't
express — e.g. "always paginated," "idempotent," "never null."

> [!IMPORTANT]
> Any breaking changes to these contract signatures require a Semantic Versioning (SemVer) MAJOR version increment.

## <endpoint or interface name>
- Signature: ...
- Invariants: ...
- Error modes: ...
