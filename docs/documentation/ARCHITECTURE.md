# System overview
Diagram or prose: major components and how data/control flows between them.

# Why it's shaped this way
Pointer back to docs/design/DECISIONS.md for the load-bearing choices — don't duplicate, link.

# Where to start reading the code
For a newcomer (human or LLM) with no context: entry points, in order.
