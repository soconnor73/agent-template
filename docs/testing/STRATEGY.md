# Test pyramid for this project
What's unit vs integration vs e2e, and where the line is drawn (and why).

# What we don't test
Explicitly out of scope (e.g., third-party SDK internals) — stops Claude from adding low-value tests.

# Mocking & Stubbing Conventions
Guidelines for isolation (e.g., "Mock all external network requests," "Use memory DB for integration testing," "Do not mock internal helper modules").

# Acceptance criteria format
Template each feature's "definition of done" should follow.

